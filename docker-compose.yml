services:
  recon:
    build: .
    ports:
      - "8088:8088"
    volumes:
      - ./reports:/app/reports
      - ./templates:/app/templates:ro
      - ./static:/app/static:ro
      - ./cli_full_scan.py:/app/cli_full_scan.py:ro
    environment:
      - OPENAI_API_KEY
    restart: unless-stopped
